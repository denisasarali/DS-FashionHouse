
@{
    ViewBag.Title = "ListaArticole";
    Layout = "~/Views/Shared/_LayoutPublicPage.cshtml";
}

@using PagedList.Mvc
@using CasadeModa.Models
@model PagedList.PagedList<Articol>


<script>
    var obj = document.querySelector(".navlist li:nth-child(1)");
    var obj2 = document.querySelector(".navlist li:nth-of-type(3)");
    obj.classList.remove("active");
    obj2.classList.add("active");
</script>
<!-- Breadcrumb Section Begin -->
<section class="breadcrumb-blog set-bg" data-setbg=@Url.Content("~/Content/img/rochierosie.jpg")>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2>Blogul nostru</h2>
            </div>
        </div>
    </div>
</section>
<!-- Breadcrumb Section End -->
<!-- Blog Section Begin -->
<section class="blog spad">
    <div class="container">
        <div class="row">
            @foreach (var i in Model)
            {
                <div class="col-lg-4 col-md-6 col-sm-6">
                    <div class="blog__item">
                        <div class="blog__item__pic set-bg" data-setbg=@Url.Content(""+@i.Imagine)></div>
                        <div class="blog__item__text">
                            <span><img src="~/Content/img/icon/calendar.png" alt=""> @i.DataPublicare</span>
                            <h5>@i.Titlu</h5>
                            <a href="@Url.Action("DetaliiArticole","Articol",new { id = i.IdArticol})">Citeste articolul</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
    <div class="product__pagination">
        @Html.PagedListPager(Model, page => Url.Action("ListaArticole", new { page = page }))
    </div>
</section>

