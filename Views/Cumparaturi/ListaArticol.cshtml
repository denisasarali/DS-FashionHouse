@using CasadeModa.Models
@using PagedList.Mvc
@model PagedList.PagedList<Produ>
<div class="row">

    @if (Model.Count == 0)
    {
<p>Nicio potrivire</p> }
else
{
foreach (var i in Model)
{
<div class="col-lg-4 col-md-6 col-sm-6">
    <div class="product__item sale">
        <div class="product__item__pic set-bg" data-setbg=@Url.Content(""+i.Imagine)>


        </div>
        <div class="product__item__text">
            <h6><a href="@Url.Action("DetaliiProdus", "Cumparaturi", new { id = i.IdProdus ,categorie = i.IdCategorie})" class="numeProdus">@i.NumeProdus</a></h6>
            <a href="@Url.Action("DetaliiProdus", "Cumparaturi", new { id = i.IdProdus, @strURL = Request.Url.ToString() })" class="add-cart">+Adauga in cos</a>
            
            <h5>


                <span class="pret">@i.Pret</span>
                <span>lei</span>

            </h5>
            @*<div class="product__color__select">
                <label for="pc-7">
                    <input type="radio" id="pc-7">
                </label>
                <label class="active black" for="pc-8">
                    <input type="radio" id="pc-8">
                </label>
                <label class="grey" for="pc-9">
                    <input type="radio" id="pc-9">
                </label>
            </div>*@
        </div>
    </div>
</div>}
}



</div>
<div class="row">
    <div class="col-lg-12">
        <div class="product__pagination">
            @Html.PagedListPager(Model, page => Url.Action("ListaProduse", new { page = page, categories = ViewBag.categorii }))
        </div>
    </div>
</div>
